define([], function () {
  return {
    PropertyPaneDescription: 'Configure sponsored/promoted results that appear when search keywords match.',
    BasicGroupName: 'Sponsored Results Configuration',
    ListNameFieldLabel: 'SharePoint list name',
  };
});
